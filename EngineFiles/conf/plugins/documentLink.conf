table    = Groups
tmplTable = templates
template = plugins/documentLink.tmpl
rootID   = 1
link     = parse.php  // not use
target   = rightFrame // not use

checkArray = check
delField = ID

xTable    = DocLinks
xAddField = "DocID,GrpID"
